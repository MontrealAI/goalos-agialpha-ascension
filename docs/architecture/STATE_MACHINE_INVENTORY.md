# State Machine Inventory

The current release inventory classifies workflow-bearing contracts and mutating selectors from tracked Solidity sources. This document is intentionally claim-bounded: it points to `qa/mainnet-readiness/system-inventory.json` and `qa/lifecycle-selector-policy.json`; it does not assert that every nonterminal state has a completed typed Solidity override until the five-gate reports are all `PASS`.
