# Layout QA v81

The smoke-test workflow runs `scripts/visual_layout_check_v81.js` with Playwright across 320, 375, 768, 1024, and 1440 pixel viewport widths.

This source package passed deterministic build and static verification locally. Browser layout QA is included in CI and must pass before merge/deploy.
