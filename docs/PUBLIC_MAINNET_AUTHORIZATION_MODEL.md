# Public Mainnet Authorization Model

Public YES is evidence-computed from committed public artifacts and public CI. It authorizes a manual local deployment package only. It does not mean deployed, externally audited, legally approved, tax reviewed, guaranteed safe, guaranteed non-security, investment, yield, or revenue share. Runtime RPC/key remain private at execution time.
