# Documentation maintenance

Run `npm run docs:deployment:build` after changing package scripts, schemas, state machine, or deployment CLI. Run `npm run docs:deployment:check` before review. Deployment documentation must preserve claim boundaries and must not include secrets.
