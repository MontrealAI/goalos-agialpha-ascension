# Optional Private Operator Package

A private operator package may help a deployer execute a real deployment, but it is optional and is not required for public repository authorization YES. Public authorization uses `qa/mainnet-authorization-certificate.json`.
