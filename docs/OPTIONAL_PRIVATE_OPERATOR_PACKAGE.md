# Optional Private Operator Package

The private operator package is optional. It can help an operator execute a real deployment, but it is not required for public repository authorization.

Public authorization comes from public CI, public evidence, public governance, public branch protection or public risk acceptance, and public release/decision artifacts. Runtime RPC/key and runtime addresses are required only for actual broadcast.
