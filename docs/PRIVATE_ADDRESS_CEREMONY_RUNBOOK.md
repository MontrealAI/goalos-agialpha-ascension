# Private Address Ceremony Runbook

Founder, treasury, deployer, admin, vault, security, and community addresses are private operator data. Keep raw addresses in `.private/address-ceremony-private.json` and publish only `addressCeremonyCommitmentHash` in redacted evidence.
