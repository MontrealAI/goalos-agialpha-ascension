# Audit Findings Register

See `AUDIT_FINDINGS_REGISTER.csv` and each timestamped `audit/reports/*/unresolved-findings.csv` for machine-readable findings. Environment-blocked audit tools are treated as mainnet blockers until run or formally accepted by qualified reviewers.
