# Contributing

Do not fabricate deployment, verification, audit, production activation, or user-fund evidence. Generated Mainnet docs must be updated with `npm run docs:mainnet:write` and checked with `npm run docs:all`. Preserve private/operator boundaries and do not commit secrets or `.private/` material.
