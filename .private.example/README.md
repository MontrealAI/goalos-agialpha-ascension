# Private operator folder template

Real `.private/` files are generated locally and must never be committed. Use `npm run deploy:mainnet:prepare-local` to create local-only templates under `.private/`.
